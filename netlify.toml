# Next.js on Netlify: publish must be ".next" (not empty — empty defaults to repo root and breaks the plugin)

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "22"
