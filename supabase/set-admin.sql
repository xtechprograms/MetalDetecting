-- Treasure Atlas | Optional: set-admin.sql
-- Run after schema.sql (step 1) to promote your account to admin
-- Edit the username below before running (not part of the main 1–17 migration order)

-- Make yourself the first admin (change username to yours!)
-- UPDATE public.profiles SET role = 'admin' WHERE username = 'your_username';
